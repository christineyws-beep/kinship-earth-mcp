[project]
name = "kinship-shared"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0", "aiosqlite>=0.20"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
